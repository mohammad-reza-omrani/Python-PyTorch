import torch  # Import the torch library

# Create two tensors filled with ones of size (1, 2), and add them together
tensor1 = torch.ones(1, 2)
tensor2 = torch.ones(1, 2)
result = tensor1 + tensor2

# Print the result
print(tensor1)
print(tensor2)
print(result)